

<div id="wrapper">
  <div id="topbar">
      <span id="header_email"><span id="account_header">Account: </span><%= current_user.email %></span>
      <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete, :id => 'signout_link' %>
      
  </div>

  <div id="main_area">

 
    <table id="sites_table">
      <tr>
	<th>Care Topic</th>
	<th></th>
	<th></th>
	<th>Clicks to Date</th>
      </tr>

    <% @sites.each do |site| %>
      <tr>
	<td><%= site.name %></td>
	<td><%= site.description %></td>
	<td><%= site.url %></td>
	<td><%= link_to 'Show', site %> &nbsp;&nbsp;&nbsp;&nbsp;<%= link_to 'Edit', edit_site_path(site) %> &nbsp;&nbsp;&nbsp;&nbsp;<%= link_to 'Delete', site, confirm: 'Are you sure?', method: :delete %></td>
      </tr>
    
	
    <% end %>
    </table>

    <br />

    <%= link_to 'New Site', new_site_path %>
    
  </div>

 


